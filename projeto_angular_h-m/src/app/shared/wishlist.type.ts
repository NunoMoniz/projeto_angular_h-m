export type Wishlist = {
    id?: number,
    name: string,
    email: string,
    password: string,
    address: string,
    postcode: string,
    country: string,
    wishlist_productsid: number[]
}